name: Crash report
description: Report bugs that cause the engine to crash here
labels: ["Type: Crash", "Awaits labels"]
body:
  - type: textarea
    id: description
    attributes:
      label: "Describe your crash here."
    validations:
      required: true

  - type: textarea
    id: terminal
    attributes:
      label: "Terminal logs (latest.log)"
      render: bash
    validations:
      required: false

  - type: textarea
    id: modding
    attributes:
      label: "Are you modding your build? If so, enter the mods/scripts installed."
    validations:
      required: true  
  
  - type: input
    id: winmario
    attributes:
      label: "In which version of WinMario this occurs in (if it's the build from the Actions tab, then also include the commit hash of the build)"
      placeholder: "3.2.1"
    validations:
      required: true

  - type: input
    id: winmario
    attributes:
      label: "For mobile: Enter here your UserID (you can find it in latest.log)"
      placeholder: "123" 

  - type: input
    id: pslice-version
    attributes:
      label: "Which \"P-Slice\" version of WinMario this occurs in"
      placeholder: "3.2.1"
    validations:
      required: true

  - type: input
    id: psych
    attributes:
      label: "Which \"Psych Engine\" version of WinMario this occurs in"
      options:
        placeholder: "3.2.1"
    validations:
      required: true

  - type: dropdown
    id: btarget
    attributes:
      label: "What platform did the issue occur?"
      options:
        - "Windows"
        - "Linux"
        - "Mac"
    validations:
      required: true



